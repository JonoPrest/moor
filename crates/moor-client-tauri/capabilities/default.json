{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may use core APIs (events, for `view`); app commands are allowed by default.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
